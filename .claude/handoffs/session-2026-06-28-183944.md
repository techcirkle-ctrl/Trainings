# Session Handoff

**Date:** 2026-06-28 18:39
**Session:** Week 2 planning
**Status:** complete (Session 2 of 3)
**Previous handoff:** session-2026-06-28-174827.md (Session 1 — superseded; this is cumulative)

> Building Week 2 (Agentic Deep Dive) across **3 sessions, ONE commit at the very end** (Session 3). Handoffs carry context; all files live UNCOMMITTED in the working tree on branch `feature/week-2`. **Session 1 = pre-read** (done). **Session 2 = audience deck** (done, this session). **Session 3 = presenter deck + homework + glossary + final audit + the single commit.**

## Decisions Made (locked — do not re-litigate)
- **Deliverables = Week-1 trio:** pre-read + audience deck + presenter deck (presenter = audience slides + per-slide notes). Homework = thin tail of the deck's closing build on each leader's OWN problem, NOT a heavy separate artifact and NOT a ready-made agent.
- **Coverage = 5 concepts** (all of slide 7, ends on synthesis): 1 agent loop · 2 workflow-vs-agent (least autonomy) · 3 tools+MCP · 4 memory & state · 5 design = the lifecycle → build.
- **Spine:** "autonomy is a cost, not a prize — least autonomy that works wins" (W1's "cheapest layer wins", one level deeper).
- **4 ready-made agents = DEMO assets** → selectable spec sheets in the deck (slide-37 style copyable folds). Drawn from W1 knowledge-base examples: vendor-contract analyser (Finance) · ECR triage · customer-escalation triage · meeting-to-decisions tracker. Audience picks one → built live in Hr 5.
- **Tools/MCP framing:** 2 examples presenter-led in Claude Code + 1 audience builds alongside. On-glass: MCP wiring = trainer's Claude Code; leaders' no-code = Claude Projects (knowledge + connectors + human checkpoint). Don't over-promise no-code tool-looping (PLAYBOOK §7.5).
- **Build tool = Claude chat + Claude Projects** (no-code); trainer demos Claude Code. NOT Gems.
- **Session shape → 5 hrs:** Hr1 debrief+cold-open · Hr2 Act 1 (loop+architectures) · Hr3 Act 2a Tools (2 presenter + 1 audience) · Hr4 Act 2b Memory · Hr5 the build (lifecycle + build a selected agent).
- **W1→W2 seam:** re-enter at W1's verbatim "agent = LLM + tools + a loop" line; explicit "From Week 1" callbacks at loop / memory(desk) / cheapest-layer→least-autonomy + data-sensitivity.
- **DECK reconciliation (PLAYBOOK §7):** session artifact = audience deck + presenter deck (NOT 3 act-decks + hands-on-movements.md helper). Hands-on prompts INLINE behind copy buttons. Shipment wins.

## Files Changed (all UNCOMMITTED — single commit at end of Session 3)
- `ai-capability-program/weeks/week-2-agentic-deep-dive/01-workshop/deck-week2-agentic-audience.html` — **NEW this session, built & verified.** 30 slides, self-contained (inline CSS + vanilla JS). Engine + chrome + mini-agenda + register-coding (act/dark/isnow) + capstone-style stepper + slide-37 spec-sheet UI (`details.promptfold` > `summary` + `.promptblock` copy) all ported from W1 audience deck. 5 hero widgets ported verbatim+sized-up from the pre-read: agent-loop graph (loop counter), pattern-picker (7 patterns + autonomy bar), tool-call stepper (5 steps), context-desk (overflow/pin/persist), lifecycle (5 stages) + spec-composer (5 guardrail states). Nav → / ← / Home / End / N (notes) / F. Print CSS included. Slide map: title·timed-agenda·mini-agenda·how-today·coldopen×2 (1–6) · Act1 divider + M1 dark/graph/keep-this + M2 dark/picker/xl (7–13) · Act2 divider + M3 dark/tool-stepper/MCP-hands-on + M4 dark/desk (14–19) · Build divider + Step1 dark/lifecycle+spec + Step2 dark/spec-sheets + Step3 dark/assemble-stepper + Step4 dark/test-stepper (20–28) · Stuck + Close (29–30).
- `ai-capability-program/weeks/week-2-agentic-deep-dive/00-preread/week-2-prework.html` — Session 1 NEW, built & verified. 5 sections. localStorage key `acp-w2-preread`. (Unchanged this session — it is the widget source-of-truth the deck ports from.)
- `ai-capability-program/RESOURCES.md` — Session 1: added R10 (DLAI Agentic Design Patterns), R11 (Claude Tool Use), R12 (MCP), R13 (in-house ReAct/Plan-Execute reading). URLs still flagged approximate — pin before Week-0 email.
- `.claude/static-server.js` — Session 1 NEW. Host static server for preview verification (node, serves repo root by absolute path, port 8765).
- `.claude/launch.json` — Session 1: python→`/opt/homebrew/bin/node .claude/static-server.js`. **Reverted to pre-session state at end of this session** (temp w2deck-static/8767 config used for verification was removed). Currently single `prework-static` config on 8765.
- `.claude/handoffs/INDEX.md` — updated (this skill).

## Verification done (audience deck — this session, twice)
- Served via host node server (sandbox disabled, bg). 8765 was held by ANOTHER chat's `prework-static` server, so used a temp port-8766 host server + temp launch config on 8767 for the preview-managed browser, navigated preview to localhost:8766 via `preview_eval`. All temp scaffolding (port-8766/8767 server copies + launch config) removed afterward; the other chat's 8765 server left untouched.
- Programmatic harness drove every widget: agent-loop 3 loops + DONE + reset ✓; pattern-picker ReAct@64% / chain / multi@94% map correctly ✓; tool stepper 5-step ends "model continues" ✓; desk add→2-dropped-on-overflow→pin→new-chat-persistence (1 cabinet | 1 loaded) ✓; lifecycle 5 stages incl Monitor ✓; spec-composer ALL 5 guardrail states correct (not-an-agent / unsafe-no-checkpoint / no-stopping-rule / getting-there / ready) ✓.
- **4 spec sheets deep pass:** 4 folds, each 3 blocks (Block A Project-instructions / B Knowledge / C Test-inputs) with correct subfold labels; 12 copy blocks total, all non-empty, all 12 distinct, every instruction block carries a HUMAN CHECKPOINT line; clicked all 12 copy buttons → all 12 flip to "Copied ✓" ✓.
- Engine: counter Home→1 / End→30 / arrows (1→7 deterministic) ✓; notes toggle ✓; both in-slide steppers (assemble/test) resolve to correct panel ✓.
- **Zero console errors** (both verification runs). Screenshots confirm on-canon rendering (pattern-picker light slide, dark goal-dividers, spec-sheet fold with copy block). Nothing broken; no fixes needed.

## Open Tasks (for Session 3 — the final session)
- [ ] **Build the Week 2 PRESENTER deck** — `01-workshop/deck-week2-agentic-presenter.html`. Same 30 slides as the audience deck + per-slide presenter script panel (Remember → Move(timing) → Say(verbatim) → Do(facilitation) → Bridge), docked side-by-side (S key, like W1 presenter). Carry timing per movement mapped to the 5-hr day-plan, verbatim Say, facilitation Do, and the **backup problem bank**. The audience deck's `.note` Takeaway blocks already hold the seed content per slide — expand into the full script. Mirror W1's `deck-week1-foundations-presenter.html` for the scriptpanel CSS/engine (the audience deck stripped it; copy back from W1).
- [ ] **Thin homework** — repurpose `02-homework/homework-1.md`: currently says "pick from spec sheet" (clashes with own-problem decision). Rewrite to "finish your build on YOUR Week-1 problem + 1-page design doc" (design doc shaped like the lifecycle: design→build→test→deploy→monitor; states output, steps, paste-in template, self-check bar, due date + handed in Teams + bring to Week 3, names what it feeds Week 3). Mirror W1 homework shape.
- [ ] **Glossary fix** — `reference/glossary.html`: correct `Gem` → Claude Projects (PLAYBOOK §7.2 — actively wrong; do when touching glossary for W2). Add new W2 terms: agent loop, ReAct, Plan-and-Execute, Reflection, MCP, tool calling, context window, agent lifecycle, workflow-vs-agent, stopping rule, human-in-the-loop/checkpoint.
- [ ] **Final self-audit (§4)** as teacher + student against the bar; fix any cross-reference/finale drift.
- [ ] **ONE commit** on `feature/week-2` — only when tested + perfect. This is the single commit for ALL of Week 2 (pre-read + both decks + RESOURCES + homework + glossary + static-server/launch.json). Verify static-server.js + launch.json are in the intended committed state.

## Pending Issues
- RESOURCES R10/R11/R12 URLs approximate (course landing / docs roots) — pin exact pages before Week-0 email. R13 in-house file still a 3-line stub.
- The 5 `00-preread/*.md` source files are empty stubs (titles only) — content came from registered sources + model knowledge, grounded via RESOURCES.
- Preview verification friction: port 8765 may be held by another chat's server → use a temp alt-port host server + temp launch config, then revert (recipe now proven; consider updating memory `preview-verification-setup` with the alt-port fallback). Screenshot capture races slide nav by ~1 slide (cosmetic; deterministic evals are the source of truth, not screenshots).

## Key Discussions
- Trainer is NOT the original code-owner; wants recommendations + me driving, not "you decide" questions. Building W2–W4 from scratch off slides 7/8/9 with W1 as the shipped reference.
- W1 pattern replicated: acts teach (with miniscule hands-on) → one closing project = week's outcome; unfinished part goes home. W2's closing project = build a ready-made agent to learn mechanics; homework applies the same mechanics to the leader's own problem.
- Audience deck has NO presenter script (safe to circulate); `.note` Takeaway blocks toggled by N. Presenter deck (Session 3) adds the docked scriptpanel.

## User Instructions
- ONE commit for all of Week 2, at end of Session 3, only when tested + perfect. Handoffs between sessions, NOT commits. **Do NOT commit** (reaffirmed this session).
- British spelling, executive register; self-contained HTML; local-only state; print-friendly (per DESIGN.md / LESSON-FORMAT / DECK-FORMAT).
- Token-optimize rules apply throughout.
- Verify deliverables in the browser preview before declaring done (host node server per memory `preview-verification-setup`); exercise every widget + spec sheet; check console; fix anything broken.

## Environment / Config Changes
- `.claude/launch.json` reverted to pre-session state (single `prework-static` / 8765). Temp verification files (`static-server-8766.js`, `static-server-8767.js`) created and removed this session. All host servers this session stopped at session end (8766 killed; the other chat's 8765 left running — not ours to stop). Restart a host server in Session 3 to verify the presenter deck (use an alt port if 8765 is busy).
- Memory `preview-verification-setup.md` recipe still valid; alt-port fallback is a candidate addition.

## Next Steps
1. Re-read PLAYBOOK §3b + §7 reconciliation + the W1 PRESENTER deck (`deck-week1-foundations-presenter.html`) for the scriptpanel CSS/engine + Remember→Move→Say→Do→Bridge format + backup-bank placement.
2. Build `deck-week2-agentic-presenter.html` from the audience deck's 30 slides + the per-slide script (expand the `.note` seeds; add timing/backup bank).
3. Repurpose `homework-1.md` (own-problem + 1-page lifecycle-shaped design doc). Update `reference/glossary.html` (Gem→Projects + W2 terms).
4. Final §4 self-audit (teacher + student). Verify in preview. Then the SINGLE commit on `feature/week-2`. No commit before that.

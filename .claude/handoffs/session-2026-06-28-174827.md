# Session Handoff

**Date:** 2026-06-28 17:48
**Session:** Week 2 planning
**Status:** complete (Session 1 of 3)
**Previous handoff:** none

> Building Week 2 (Agentic Deep Dive) across **3 sessions, ONE commit at the very end** (Session 3). Handoffs carry context; files live uncommitted in working tree. This is Session 1 — pre-read done. Next: audience deck.

## Decisions Made (locked — do not re-litigate)
- **Deliverables = Week-1 trio:** pre-read + audience deck + presenter deck (presenter = audience slides + per-slide notes). Homework = thin tail of the deck's closing project, NOT a heavy separate artifact.
- **Coverage = 5 concepts**, covering ALL of slide 7, nothing extra (display = cognitive order, ends on synthesis):
  1. **The agent loop** (plan→act→observe→decide; deepens W1 "agent is a loop")
  2. **Workflow vs agent — least autonomy that works** (chaining/routing/parallel = workflow; ReAct/Plan-Execute/Reflection/multi-agent = agent)
  3. **Tools** (tool-calling: model requests → your system runs → observation returns; MCP = standard plug)
  4. **Memory & state** (context window = "the desk", W1 callback; short vs long-term; persistent Projects)
  5. **Design your agent = the lifecycle** (design→build→test→deploy→monitor) → synthesis + the build
- **Slide 7 is source of truth.** Caught gap: "agent **lifecycle** (design→build→test→deploy→monitor)" is distinct from the agent *loop* — now concept 5.
- **Spine/judgement:** "autonomy is a cost, not a prize — least autonomy that works wins" (W1's "cheapest layer wins", one level deeper).
- **Homework = each leader's OWN problem**, carried all 4 weeks. The 4 ready-made agents are NOT homework.
- **The 4 ready-made agents = DEMO assets** → selectable spec sheets IN the deck UI (style of slide 37 of W1 audience deck: copyable Project-instructions + knowledge + test-inputs in `<details>` folds). Audience picks one → built live in Hr 5. Draw the 4 from W1 knowledge-base examples (ECR triage / customer-escalation triage / meeting-to-decisions tracker / vendor-contract analyser — W1 audience deck lines ~830–846) for continuity.
- **Tools/MCP framing:** presenter demos live from laptop (Claude Code) — **2 examples presenter-led + 1 audience builds alongside**. Literacy + show-it-can-be-built. Deck must state on-glass: MCP wiring = trainer's Claude Code surface; leaders' no-code = Project knowledge + connectors + human checkpoint (PLAYBOOK §7.5 — don't over-promise no-code).
- **Build tool = Claude chat + Claude Projects** (no-code); trainer demos Claude Code. NOT Gems.
- **Session shape (acts + closing build), mapped to 5 hrs:** Hr1 debrief+cold-open · Hr2 Act 1 (loop+architectures) · Hr3 Act 2a Tools (2 presenter + 1 audience) · Hr4 Act 2b Memory · Hr5 the build (lifecycle + build a selected ready-made agent). Two teaching acts + one closing build (mirrors W1's "teach then build").
- **W1→W2 seam (retention):** re-enter at W1's exact "agent = LLM + tools + a loop" line, then deepen. Explicit "From Week 1" callbacks at loop / memory(desk) / cheapest-layer→least-autonomy + data-sensitivity.

## Files Changed (all UNCOMMITTED — single commit at end of Session 3)
- `ai-capability-program/weeks/week-2-agentic-deep-dive/00-preread/week-2-prework.html` — **NEW, built & verified.** 5 sections; ported v2 CSS + `buildGraph` agent-loop graph + quiz engine + progress/localStorage; 4 new widgets (pattern-picker, tool-call stepper, context-desk, lifecycle+spec-composer). localStorage key `acp-w2-preread`. British spelling, exec register, print CSS, self-contained.
- `ai-capability-program/RESOURCES.md` — added R10 (DLAI Agentic Design Patterns), R11 (Claude Tool Use), R12 (MCP), R13 (in-house ReAct/Plan-Execute PE reading). URLs flagged approximate — pin before Week-0 email.
- `.claude/static-server.js` — NEW. Host static server for preview verification (see memory `preview-verification-setup`).
- `.claude/launch.json` — switched python→`/opt/homebrew/bin/node .claude/static-server.js` (python sandbox blocked getcwd; preview PATH lacks Homebrew).

## Verification done (pre-read)
- Served via Bash host node server (sandbox disabled, bg) on :8765; preview browser navigated to localhost URL.
- Programmatic harness drove every widget: agent-loop steps/loop-counter/reset ✓; pattern-picker 7 patterns map correctly (ReAct/Prompt-chaining) + autonomy bar ✓; tool stepper 5-step request→run→observe ✓; desk add→overflow-drop→pin→new-chat-persistence ✓; lifecycle 5 stages ✓; spec-composer all guardrail states correct (not-an-agent / unsafe-no-checkpoint / no-stopping-rule / ready) ✓; 5 quizzes×3 ✓; progress+localStorage ✓. **Zero console errors.** Screenshot confirms Week-1 canon match.

## Open Tasks (for Session 2 & 3)
- [ ] **Session 2: build Week 2 AUDIENCE deck** — `01-workshop/deck-week2-agentic-audience.html`. Cold open + Act 1 (loop+architectures) + Act 2 (tools + memory) + closing build act. Port the pre-read's 4 hero widgets (agent-loop graph, pattern-picker, tool stepper, context-desk, spec-composer) sized up. Include the **4 selectable spec sheets** (slide-37 copyable-block style) for the Hr-5 build. Re-teach from zero. Follow PLAYBOOK §3b + DECK reconciliation (audience+presenter, NOT 3-deck/helper).
- [ ] **Session 3: presenter deck** (audience slides + per-slide Remember→Move→Say→Do→Bridge notes + timing + backup bank) + thin homework note + glossary update.
- [ ] **Session 3: fix glossary `Gem` → Claude Projects** (PLAYBOOK §7.2 — actively wrong; do when touching glossary for Week 2). Add new W2 terms: agent loop, ReAct, Plan-and-Execute, Reflection, MCP, tool calling, context window, agent lifecycle, workflow-vs-agent.
- [ ] **Session 3: repurpose** `02-homework/homework-1.md` (currently says "pick from spec sheet" — clashes with own-problem decision) into thin "finish your build + 1-page design doc on YOUR problem" note.
- [ ] **Session 3: final self-audit (§4) + ONE commit** on `feature/week-2`.

## Pending Issues
- RESOURCES R10/R11/R12 URLs approximate (course landing / docs roots) — pin exact pages before Week-0 email. R13 in-house file is still a 3-line stub.
- The 5 `00-preread/*.md` source files are empty stubs (just titles) — content came from registered sources + model knowledge, grounded via RESOURCES.

## Key Discussions
- Trainer is NOT the original code-owner; wants recommendations + me driving, not "you decide" questions. Building W2–W4 from scratch off slides 7/8/9, with W1 as the shipped reference.
- W1 had no standalone homework — deck taught 3 acts then a ~1.5–2hr "wholesome project" (Capstone Decider); unfinished part went home. Replicate: acts teach (with miniscule hands-on) → one closing project = week's outcome.
- Focus = pre-read + session precision; smooth, frictionless pre-read→session transition; strong W1→W2 overlap for retention.

## User Instructions
- ONE commit for all of Week 2, at end of Session 3, only when tested + perfect. Handoffs between sessions, NOT commits.
- British spelling, executive register; self-contained HTML; local-only; print-friendly (all per DESIGN.md / LESSON-FORMAT).
- Token-optimize rules apply throughout.

## Environment / Config Changes
- Added `.claude/static-server.js` + repointed `.claude/launch.json` to node. Preview verification recipe saved to memory `preview-verification-setup.md` (host node server via Bash + `dangerouslyDisableSandbox`, navigate preview to localhost:8765). Server was stopped at session end — restart it in Session 2 to verify the deck.

## Next Steps
1. Re-read PLAYBOOK §3b + DECK-FORMAT reconciliation + the W1 audience deck (`deck-week1-foundations-audience.html`) — engine, mini-agenda, chrome, register coding, bridge-slide pattern, ported widgets, and slide-37 spec-sheet UI.
2. Build the Week 2 audience deck from the locked 5-concept coverage + session shape above. Port the pre-read's verified widgets.
3. Verify in preview (restart host node server per memory). No commit.
4. Handoff for Session 3.
